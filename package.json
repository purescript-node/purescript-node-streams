{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "jshint src && pulp build && rimraf docs && pulp docs"
  },
  "devDependencies": {
    "jshint": "^2.8.0",
    "pulp": "^8.2.1",
    "rimraf": "^2.4.1",
    "stream-buffers": "^3.0.0"
  }
}
